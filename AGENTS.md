# SakeMenu: 新しい日本酒を追加するAI向け手順

このファイルは、このリポジトリで新しい日本酒を追加するAIエージェント向けの作業規約です。人向けの手順は `docs/SAKE_UPDATE_GUIDE.md` を参照してください。

## 最重要ルール

- ユーザーが指定したスキップ対象の写真は、調査、画像作成、`menu.csv` への追加、状態管理シートへの追加のすべてから除外する。
- `photo/` 以下の原本は編集しない。明示的に依頼されない限り、原本写真をGitへ追加しない。
- 既存の `id` は変更しない。`id` は売切れ状態、注文、評価との紐付けに使われる。
- 銘柄を推測で確定しない。ラベルで判別できない項目は信頼できる情報源で確認し、確認できない値は空欄にする。
- ユーザーの未コミット変更を上書き、削除、または無断で同じコミットに混ぜない。
- 状態管理シートを更新するとき、既存行の `soldOut`、`orderEnabled`、`hidden` を初期値で上書きしない。

## 現在のデータ構成

- Webメニュー本体: `menu.csv`
- CSVの読込設定とキャッシュ版数: `data.js`
- ラベル画像: `labels/` または `labels/web/`
- Web表示処理: `app.js`
- GASソース: `gas/Code.gs`
- 状態管理シートの見本: `gas/menu.sample.csv`
- 公開先: `https://kerroggu.github.io/SakeMenu/`
- 酒情報管理シート: `https://docs.google.com/spreadsheets/d/1uYTCXtAmor6ZzrItT2POE25LvmWec1D75Eaco3ZaDFA/edit?gid=0#gid=0`
- GAS・状態管理用スプレッドシート: `https://docs.google.com/spreadsheets/d/1W3_a2qvZ6z6cpHRbyxMqZKdy-TH7Qubl1PHIxbGwS04/edit?gid=0#gid=0`

現在のWebサイトは酒情報管理シートを直接読まず、`menu.csv` を読む。したがって、酒情報管理シートだけを更新してもWebメニューは変わらない。シートを正本として管理する場合も、現状ではシートと `menu.csv` の同期が必要である。

GAS側の `menu` シートは酒の説明ではなく、`id,soldOut,orderEnabled,hidden` の状態を管理する。

## 追加作業の順序

### 1. 作業前の確認

1. `git status --short` でユーザーの既存変更を把握する。
2. 指定された写真フォルダを列挙し、スキップ対象を別に記録する。
3. `menu.csv` と `labels/` を検索し、既存銘柄、同じ写真、同じ `id` がないか確認する。
4. 同じボトルの表裏や撮り直し写真は1銘柄にまとめる。写真1枚を機械的に1銘柄として扱わない。

### 2. 銘柄の特定と調査

1. 元写真を拡大して、銘柄、商品名、蔵元、特定名称、酒米、精米歩合、火入れ、生酒、度数、限定品表記を読む。
2. 完全な商品名でWeb検索する。蔵元の公式サイト、公式商品ページ、信頼できる酒販店の商品ページを優先する。
3. ヴィンテージや限定ロットが違う情報を混ぜない。
4. 味わいは情報源とラベルから簡潔に要約する。確認できないスペックを一般的な銘柄情報から補完しない。
5. 調査元URLは作業報告用に控える。CSVへURL列を勝手に追加しない。

`menu.csv` で使う列は次の16列で、順序を変えない。

```text
Id,Name,SearchName,Image,Price,Tsukuri,Hiire,Sakamai,Prefecture,Shuzou,Seimai,Status,Flavor,Temperature,Pairing,Alcohol
```

- `Id`: 英小文字・数字・ハイフンを使った永続的な一意ID
- `Name`: カードに表示する短い名前
- `SearchName`: 検索で見つけるための正式名・別名を含む名前
- `Image`: リポジトリ内の相対パス
- `Status`: 表示する場合は通常 `1`
- 未確認値: 推測せず空欄
- `Pairing`: `/` 区切り

CSV中にASCIIのカンマを含む値を書く場合は、CSVとして正しくダブルクォートする。

### 3. ラベル画像の作成

1. 必ず元写真を参照した画像編集として行い、ボトルやラベルを新規生成しない。
2. ボトル正面またはラベルが中心になるようにトリミングする。
3. 傾き、露出、ホワイトバランス、色かぶりを自然な範囲で補正する。
4. 商品名、文字、ロゴ、ラベル模様を改変・捏造しない。文字が崩れた出力は不採用にする。
5. 既存カードと近い縦長比率にし、スマホ表示に十分な解像度を残す。
6. ファイル名は原則として `<id>.jpg` または `<id>.png` とする。Web用に最適化した画像は `labels/web/` を優先する。
7. 出力後に画像を目視し、向き、文字、色、余白、別銘柄との取り違えを再確認する。

### 4. メニューと状態一覧の更新

1. `menu.csv` の末尾へ新しい行を追加する。
2. `data.js` の `menuVersion` を、その更新が識別できる新しい値へ変更する。例: `2026-08-01-new-sake`。
3. `gas/menu.sample.csv` に同じ `id` を追加し、初期値を `false,true,false` にする。
4. 実際の状態管理スプレッドシートへは、新規IDの行だけを追加する。既存行をサンプルCSVで全置換しない。
5. `menu` シートの列は `id,soldOut,orderEnabled,hidden` とし、後ろ3列はチェックボックスにする。
6. 酒情報管理シートも利用可能なら、同じ銘柄情報を同期する。ただし、Web反映には引き続き `menu.csv` の更新が必要である。

新規状態行の初期値:

```csv
new-sake-id,false,true,false
```

- `soldOut=true`: 表示したまま売切れ、注文不可
- `orderEnabled=false`: 表示したまま受付停止
- `hidden=true`: Webメニューから非表示

`hidden` の処理など `gas/Code.gs` 自体を変更した場合は、保存やコミットだけでは稼働中GASへ反映されない。Apps Scriptの既存Webアプリを編集し、`新しいバージョン` としてデプロイする必要がある。酒の行を追加するだけなら、通常はGASコードの変更や再デプロイは不要である。

原則として既存デプロイを更新し、WebアプリURLを維持する。新しいデプロイを作ってURLが変わった場合は、同じ作業内で `data.js` の `gasAppUrl` を新しい `/exec` URLへ変更し、`index.html` の `data.js` 読込クエリも更新してブラウザキャッシュを回避する。

Apps Script上のコードがリポジトリより長い場合、ファイル全体をGitHub版で上書きしない。通知キューなど稼働中だけに存在する変更を確認し、対象関数だけを差し替えるか、両方を統合したコードを作ってからデプロイする。

### 5. 必須検証

公開前に最低限、次を確認する。

- `git diff --check` が成功する。
- `node --check app.js` と `node --check data.js` が成功する。
- `menu.csv` の各行がヘッダと同じ16列として読める。
- すべての `Id` が空でなく、一意である。
- 新しい各 `Image` パスに実ファイルが存在する。
- 明示されたスキップ写真に対応する行や画像が存在しない。
- ローカルHTTPサーバーからページを開き、カード画像、名前、検索、注文ボタンを確認する。
- 状態APIのテストが可能なら、`soldOut`、`orderEnabled`、`hidden` をそれぞれ確認する。
- `hidden=true` の銘柄がDOM上にも表示されないことを確認する。
- 稼働中の `gasAppUrl?action=menuStatus` を直接取得し、各項目に `hidden` が含まれることを確認する。`hidden` 自体がない場合は旧デプロイまたは旧URLである。

### 6. 公開

1. 公開まで依頼されている場合は、対象ファイルだけを明示的にステージする。`git add .` は使わない。
2. `photo/` の原本と無関係な既存変更がステージされていないことを確認する。
3. 意味の分かるコミットメッセージでコミットし、公開元の `main` へpushする。
4. GitHub Pagesの反映後、公開先の `menu.csv` のサイズまたは新規 `id` を確認する。
5. ブラウザキャッシュ対策として `menuVersion` が更新されていることを再確認する。
6. 最終報告には、追加件数、スキップ対象、シート更新の有無、GAS再デプロイの有無、commit ID、公開確認結果、残作業を記載する。

## ページが変わらない場合

次の順で切り分ける。

1. 変更がローカルだけでなく `origin/main` へpushされているか。
2. GitHub Pagesが `main` のルートから公開されているか。
3. 公開中の `menu.csv` に新規 `id` があるか。
4. `data.js` の `menuVersion` を更新したか。
5. CSVの列ずれや画像パスの大文字小文字違いがないか。
6. 10分程度のCDNキャッシュ後も古い場合は、クエリ文字列付きで公開ファイルを確認する。
7. `hidden` が効かない場合は、シート、GAS API、公開中 `data.js` の順に確認する。

`hidden` の切り分け:

1. `menu` シートの対象行が `TRUE` か。
2. `gasAppUrl?action=menuStatus` が対象 `sakeId` に `"hidden":true` を返すか。
3. APIに `hidden` がなければ、Apps Scriptの新バージョン未デプロイまたはページが旧URLを参照している。
4. APIが正しくても表示される場合は、公開中 `data.js` の `gasAppUrl` とブラウザキャッシュを確認する。

酒情報管理シートの更新だけでは、現在のページ本体は更新されない。

## Sheetsへのアクセス

シートを直接編集するには、Google Driveプラグイン／コネクタをユーザーのGoogleアカウントで認証し、接続が見える新しいCodexセッションを使う。Codex用メールアドレスへの共有はできない。「リンクを知っている全員を編集者」に変更するよう依頼してはならない。接続できない場合は、変更するセルや追記用CSVを提示し、ユーザーに安全な手動更新を依頼する。
